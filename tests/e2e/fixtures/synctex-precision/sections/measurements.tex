Inline math keeps line mapping stable when the formula is short.
Equation content on a single line: $E = m c^2$ and $a+b=c$.
A second inline formula includes indices: $x_i = y_i + z_i$.
Only one math expression should be sufficient per line.
Spacing and punctuation are intentionally varied for deterministic parsing.
This line mentions markers: MEASUREMENTS\_001 and MEASUREMENTS\_002.
Avoid display math here so the mapping stays easy to verify.
The sentence below is the final measurements line for this section.
