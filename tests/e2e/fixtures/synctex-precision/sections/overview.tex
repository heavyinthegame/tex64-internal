Scenario-driven tests keep coordinates repeatable.
A clean source file produces stable forward and reverse mappings.
Each line of this fixture is plain prose for line-level validation.
Do not add unsupported macros in this section.
We intentionally keep this text deterministic.
Even short lines should still map correctly when synchronized.
The sync engine should navigate to the exact source line requested.
Plain sentence blocks make this check robust across engines.
This section is used to catch off-by-one regressions quickly.
A unique marker: OVERVIEW\_MARKER\_ALPHA.
Another marker: OVERVIEW\_MARKER\_BETA.
